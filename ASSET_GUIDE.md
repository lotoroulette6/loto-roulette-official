# 正式画像とURLの差し替えガイド

現状のレイアウト・動作を維持したまま、正式素材を用意するための一覧です。現時点では画像は未配置、5種類のリンクは空欄、公開URLの設定はHTMLコメント内で無効になっています。GitHubへの送信・公開は行いません。

## 画像9点の準備一覧

保存先はすべて `loto-roulette-official` フォルダを基準にしています。サイズは横 × 縦、比率は横：縦です。すべてPNG形式で用意してください。

| 画像 | ファイル名 | 保存先フォルダ | 推奨ピクセルサイズ | 推奨縦横比 | サイト内の使用箇所 |
| --- | --- | --- | --- | --- | --- |
| ロトルーレット正式ロゴ | logo-main.png | images/logo/ | 760 × 160 | 19：4 | ヘッダー左上。フッターは現在の文字ロゴを維持 |
| モード選択画面 | mode-select.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | FIRST VIEW、HOW TO PLAYの「モード選択」（同じ画像を共用） |
| LOTO7プレイ画面 | loto7.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | ABOUT、ALL IN ONEのLOTO7カード（同じ画像を共用） |
| LOTO6プレイ画面 | loto6.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | ALL IN ONEのLOTO6カード |
| MINIプレイ画面 | mini.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | ALL IN ONEのMINIカード |
| 回転中画面 | spinning.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | HOW TO PLAYの「回転中」 |
| 数字確定画面 | number-confirmed.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | HOW TO PLAYの「数字確定」 |
| 全数字完成画面 | completed.png | images/app/ | 780 × 1560 ※ | 1：2 ※ | HOW TO PLAYの「完成」 |
| OGP画像 | og-image.png | images/og/ | 1200 × 630 | 40：21（約1.91：1） | SNSでサイトURLを共有した際の画像。本文には表示されない。公開URL設定後に有効化 |

※ アプリ画面の数値は、現在のサイト表示枠（1：2）に合わせて書き出す場合の目安です。**iPhoneで撮影したスクリーンショットは元のサイズ・比率のままでも使用できます。** 1：2に合わせるために画面を引き伸ばしたり、上下のUIを切り取ったりしないでください。実際の画面比率を優先し、7枚のスクリーンショットは同じ端末・同じ解像度で揃えることをおすすめします。

現在のCSSは画像全体を表示する設定（object-fit: contain）です。比率が違う場合は枠内に余白が入ります。画像枠は最大幅250px、高さ500pxなので、780 × 1560は高精細表示にも余裕のある制作サイズです。今回はCSSや画像枠を変更していません。

ロゴの表示枠は190 × 40pxです。透明背景を推奨します。余分な透明余白を少なくし、ロゴ本体の比率を維持してください。正式ロゴの形が大きく異なる場合は、素材受領後に枠への収まりを確認します。

## 画像を置く手順

1. 原本を別途保管し、サイト用のコピーを用意します。
2. コピーのファイル名を上の表と完全に同じ名前にします。大文字・小文字も一致させ、拡張子が `.png.png` にならないようにしてください。JPEGの名前だけをPNGに変えず、PNG形式で書き出してください。
3. 指定フォルダに保存します。画面画像7枚はすべて `images/app/` です。`images/screenshots/` は現在参照されていません。
4. `index.html` をブラウザで開いて再読み込みします。ロゴ・画面画像はファイルが読み込めると自動で準備中表示から切り替わります。OGP画像だけは別途URL設定が必要です。
5. PCとスマホ幅で、文字の読みやすさ・余白・画面の順序を確認します。モード選択画面はLOTO7 → LOTO6 → MINIの順にしてください。

回転中・数字確定・完成の3枚は同じモード（例：LOTO7）で揃えると流れが伝わります。完成画面は必要数が揃い、小さい順に整列した状態を用意してください。

## 用意する公式リンク5種類

編集するファイルは `js/main.js`、場所は冒頭の **`const SITE_LINKS = {`** から始まるブロックです。現在は全項目が空欄です。

| 用意するURL | 編集するキー | 反映先 |
| --- | --- | --- |
| 正式なApp Storeのアプリ紹介ページ | appStore | FIRST VIEW、ALL IN ONE下、APP DOWNLOAD、フッター |
| 公式Xのプロフィール | x | 最新情報のXボタン、フッター |
| 公式YouTubeチャンネル | youtube | 最新情報のYouTubeボタン、フッター |
| 公開済みプライバシーポリシー | privacy | フッター |
| お問い合わせページ、または連絡用メール | contact | フッター |

半角の引用符 `''` の間に、確認済みURLだけを1行で入力します。キー名、引用符、カンマ、波括弧は削除しないでください。未確定項目は `''` のまま残します。URLの前後に空白や改行を入れないでください。

お問い合わせ以外は `https://` で始まるURLを入力します。お問い合わせも通常はHTTPSのページURLで、メールの場合は `mailto:` に続けて正式なメールアドレスを入力できます。URL内に半角のシングルクォートが含まれる場合は、そのまま貼り付けず `%27` に置き換えてください。

現在のコードはURLの冒頭の形式を確認してリンクを有効にしますが、URLの存在や公式アカウントかどうかは判定しません。貼り付け前に実際に開いて確認してください。入力後は再読み込みし、設定したすべてのリンクを開いて目的のページに着くことを確認します。空欄の項目は引き続き「リンク準備中」です。

## 公開URLの設定（公開先が確定した後）

5種類のリンクとは別に、**このサイトの正式なトップページURL**が必要です。現在は未確定なので設定しません。

編集するファイルは `index.html`、場所は冒頭の **`TODO: 公開URL設定`** コメントです。

| 項目 | 書き換える属性 | 入れる内容 |
| --- | --- | --- |
| canonical | `<link rel="canonical">` の href | 正式なトップページの絶対URL |
| og:url | `<meta property="og:url">` の content | canonicalと同じ正式なトップページURL |
| og:image | `<meta property="og:image">` の content | 公開されたOGP画像の絶対URL |

1. 正式な公開URLとOGP画像が確定するまでは、コメントをそのまま残します。
2. 確定後、コメント内の3つのタグ行だけを `-->` の直後へ移します。説明文はコメント内に残します。
3. canonicalとog:urlの属性値を、末尾に `/` がある正式トップページURL全体で置き換えます。GitHub Pagesでリポジトリ名がURLに含まれる場合は、それも省略しません。
4. og:imageの属性値全体を「上記の末尾 `/` 付きURL ＋ `images/og/og-image.png`」で置き換えます。`/` を二重にしないでください。
5. HTML内に有効な `TODO_PUBLIC_URL` が残っていないことを確認します。ローカルファイルの場所やlocalhostのURLは入力しません。

OGP URLには、共有するページのURL（og:url）と共有画像のURL（og:image）の2種類があります。画像を置くだけではOGP設定は有効になりません。Twitter Cardの種類・タイトル・説明は既に設定済みで、共有画像にはOGP画像を使用する構成です。

## その後の次工程

まず正式画像9点と公式リンク5種類を揃え、配置・入力後にローカルでPCとスマホ表示を確認します。その後、公開先が決まった時点でcanonical・OGPを設定します。GitHubへのpushとGitHub Pages公開は、別途公開を指示いただいた後の工程です。
